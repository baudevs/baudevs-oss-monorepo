name: "Release with Version Plan"

on:
  workflow_call:
    inputs:
      projects:
        description: "List of project names"
        required: true
        type: string
    secrets:
      NX_TOKEN:
        required: true
      NPM_TOKEN:
        required: true
      GH_PAT:
        required: true

env:
  NX_EXCLUDED_PROJECTS: 'baudevs-dashboard,@baudevs/bauCmsNextExample'
  NODE_VERSION: 23.3.0
  PNPM_VERSION: 9.15.2

jobs:
  release-projects:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        project: ${{ fromJson(inputs.projects) }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          registry-url: 'https://registry.npmjs.org'

      - name: Setup Pnpm
        uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Install Dependencies
        run: pnpm install

      - name: Execute Release
        run: |
          cd tools/release-tools
          echo "Releasing project: ${{ matrix.project }}"
          pnpm tsx src/cli.ts release \
            --project="${{ matrix.project }}" \
            --skip-publish
          cd ../../

      - name: Publish Package
        run: |
          cd tools/release-tools
          echo "Publishing project: ${{ matrix.project }}"
          pnpm tsx src/cli.ts release \
            --project="${{ matrix.project }}"
          cd ../../
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
